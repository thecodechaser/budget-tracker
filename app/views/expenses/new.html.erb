<div class="flex flex-col items-center mb-20">
<div class="flex w-96 bg-sky-500 p-4 text-slate-50">
<%= link_to group_expenses_path(params[:group_id]) do %>
  <%= image_tag ('back-icon.png'), class: "w-6 h-6" %>
  <% end %>
    <h3 class="ml-28">CREATE</h3>
</div>
  <h3 class="text-sky-400 text-xl font-bold mb-6 mt-4"> Add a new transaction</h3>
  <%= form_with url: group_expenses_path(params[:group_id]), scope: :expense do |form| %>
    <div>
    <%= form.label :Name %><br>
    <%= form.text_field :name, class: 'mt-2 w-56 p-1 rounded', require: true %>
    </div>

    <div class="mt-2">
    <%= form.label :amount %><br>
    <%= form.number_field :amount, class: 'mt-2 w-56 p-1 rounded', require: true %>
    </div>

    <%= form.submit "Submit", class: "bg-sky-400 p-2 rounded text-slate-50 mt-4" %>
    <%= link_to 'Back', group_expenses_path(params[:group_id]), class: "bg-sky-400 rounded text-slate-50 mt-4 ml-20 px-4 py-3" %>
  <% end %>
</div>